'use strict';

var _reactErrorOverlay = require('react-error-overlay');

var _launchEditorEndpoint = require('react-dev-utils/launchEditorEndpoint');

var _launchEditorEndpoint2 = _interopRequireDefault(_launchEditorEndpoint);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/* eslint-disable */
(0, _reactErrorOverlay.setEditorHandler)(function (errorLocation) {
  // Keep this sync with errorOverlayMiddleware.js
  fetch(_launchEditorEndpoint2.default + '?fileName=' + window.encodeURIComponent(errorLocation.fileName) + '&lineNumber=' + window.encodeURIComponent(errorLocation.lineNumber || 1) + '&colNumber=' + window.encodeURIComponent(errorLocation.colNumber || 1));
});

(0, _reactErrorOverlay.startReportingRuntimeErrors)({
  onError: function onError() {
    module.hot.addStatusHandler(function (status) {
      if (status === 'apply') {
        window.location.reload();
      }
    });
  }
});